import { MglMapResizeDirective } from './mgl-map-resize.directive';

describe('MglMapResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new MglMapResizeDirective();
    expect(directive).toBeTruthy();
  });
});
